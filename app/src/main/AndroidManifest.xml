<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Timewise"
        tools:targetApi="31">

        <activity
            android:name=".notes.EditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".notes.NotesActivity"
            android:exported="false" />
        <activity
            android:name=".followGoal.GoalsActivity"
            android:exported="false" />
        <activity
            android:name=".followGoal.AddGoalActivity"
            android:exported="false" />

        <receiver
            android:name=".followEfficiency.AlarmReceiver"
            android:exported="false" />

        <activity android:name=".followEfficiency.EfficiencyActivity" />
        <activity
            android:name=".followGoal.GoalDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".calender.CalendarActivity"
            android:exported="false" />
        <activity
            android:name=".enterApp.School"
            android:exported="false" />
        <activity
            android:name=".todo.Todos"
            android:exported="false" />
        <activity
            android:name=".schoolSchedule.SchoolSchedule"
            android:exported="false" />
        <activity
            android:name=".task.TasksScreen"
            android:exported="false" />
        <activity
            android:name=".users.UserListDisplay"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".UserListDisplay" -->
        <!-- android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".enterApp.HomeScreen"
            android:exported="false" />
        <activity
            android:name=".enterApp.SignInUser"
            android:exported="false" />
        <activity
            android:name=".enterApp.log_in"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>